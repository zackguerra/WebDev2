
//  runs only once
function setup() {
    createCanvas(400, 300);
    // x-axis and y-axis
}

// runnning again again again
function draw() {
    background(220, 0, 200);
    // line(0, 50, 400, 300);
    rect(0, 0, 50, 50)

    // x-axis, y-axis, width, height
}